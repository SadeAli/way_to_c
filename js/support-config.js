/* ============================================================
   support-config.js — Stage-0 monetization switches.
   EVERY surface below stays completely hidden until you fill in
   the matching value, so the site never shows a broken link.
   Fill these in as you create the accounts (all free):
   ============================================================ */
window.SUPPORT = {
  /* github.com/sponsors — enable on your GitHub account, then put
     your username here, e.g. 'ali'. Renders "♥ Sponsor" links. */
  githubSponsors: '',

  /* ko-fi.com — create a page, put the page name here, e.g. 'thecpath'. */
  kofi: '',

  /* buttondown.com — create a (free <100 subs) newsletter, put the
     username here, e.g. 'thecpath'. Renders the email opt-in form. */
  buttondown: '',

  /* Amazon Associates tag, e.g. 'thecpath-20'. When empty, the books
     page renders plain (non-affiliate) Amazon links. */
  amazonTag: '',

  /* Names shown on the supporters wall (#/support). Hand-edit after
     each sponsorship — this is the static "wall of thanks". */
  supporters: [],
};
